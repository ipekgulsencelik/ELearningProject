
@{
    Layout = "~/Views/Shared/_StudentLayout.cshtml";
}

@*
<h3>Profil Sayfası</h3>

<h4>Mail Adresi: @ViewBag.mail</h4>

<h5>Hoşgeldin @ViewBag.name</h5>
*@

<div class="row">
    <!-- Column -->
    @Html.Action("_StudentPanelPartial", "Profile")
    <div class="col-lg-8 col-xlg-9 col-md-7">
        <div class="card">
            <!-- Nav tabs -->
            <ul class="nav nav-tabs profile-tab" role="tablist">
                <li class="nav-item"> <a class="nav-link active" data-toggle="tab" href="#home" role="tab">Yorumlar</a> </li>
                <li class="nav-item"> <a class="nav-link" data-toggle="tab" href="#profile" role="tab">İletişim</a> </li>
                <li class="nav-item"> <a class="nav-link" data-toggle="tab" href="#settings" role="tab">Kurs Listesi</a> </li>
            </ul>
            <!-- Tab panes -->
            <div class="tab-content">
                @Html.Action("_CommentPartial", "Profile")

                @Html.Action("_ContactPartial", "Profile")

                @Html.Action("_CoursePartial", "Profile")
            </div>
        </div>
    </div>
</div>